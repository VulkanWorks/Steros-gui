#include <app.h>
#include <ui/button.h>

int main(void) {
//  uint32_t count = 0;
//  vkEnumerateInstanceExtensionProperties(NULL, &count, NULL);
//  VkExtensionProperties *extensions = malloc(sizeof(VkExtensionProperties) * count);
//  vkEnumerateInstanceExtensionProperties(NULL, &count, extensions);
//
//  for(uint64_t i = 0; i < count; i++) {
//    puts(extensions[i].extensionName);
//  }

//  strs_string title = strs_string_create_from_cstr("Title", 6);
//  strs_window window = strs_window_create(800, 600, &title);
//
//  uint64_t test1 = 0;
//  uint64_t test2 = 0;
//
//  while(strs_window_closing(window) == false) {
//    strs_window_poll_events(window);
//    strs_window_get_size(window, &test1, &test2);
//  }
//
//  printf("%lu\n", test1);
//
//  strs_window_free(window);
}

